from distutils.core import setup
setup(
  name = 'pocketcasts',
  packages = ['pocketcasts'], # this must be the same as the name above
  version = '0.1',
  description = 'python module to interact with the unofficial Pocket Casts API.',
  author = 'Tobias Jost',
  author_email = 'tobias.jost@spinweb.de',
  url = 'https://github.com/sirl1on/python-pocketcasts', # use the URL to the github repo
  download_url = 'https://github.com/sirl1on/python-pocketcasts/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['pocketcasts', 'api', 'podcast'], # arbitrary keywords
  classifiers = [],
)
